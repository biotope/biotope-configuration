import createConfigurationLoader from './createConfigurationLoader';

(<any>window).biotope = (<any>window).biotope || {};
(<any>window).biotope.createConfigurationLoader = createConfigurationLoader;
